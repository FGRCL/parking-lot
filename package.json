{
  "scripts": {
    "build": "mkdir dist && cp src/*.html dist/ && npx webpack"
  },
  "dependencies": {
    "@googleworkspace/meet-addons": "^1.2.0"
  },
  "devDependencies": {
    "webpack": "^5.101.0",
    "webpack-cli": "^6.0.1"
  }
}
